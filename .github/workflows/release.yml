name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to release (example: v0.1.0-alpha.0)"
        required: true
      prerelease:
        description: "Mark release as pre-release"
        required: false
        default: "true"

permissions:
  contents: write

jobs:
  build-assets:
    name: build ${{ matrix.platform }}-${{ matrix.arch }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
            arch: x86_64
            target: x86_64-unknown-linux-gnu
          - os: macos-14
            platform: darwin
            arch: x86_64
            target: x86_64-apple-darwin
          - os: macos-14
            platform: darwin
            arch: aarch64
            target: aarch64-apple-darwin

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        run: rustup toolchain install stable --profile minimal

      - name: Add Rust target
        run: rustup target add ${{ matrix.target }}

      - name: Build release binary
        run: cargo build --release --target ${{ matrix.target }} --locked || cargo build --release --target ${{ matrix.target }}

      - name: Package release asset
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          cp "target/${{ matrix.target }}/release/speq" dist/speq
          chmod +x dist/speq
          tar -czf "dist/speq-${{ matrix.platform }}-${{ matrix.arch }}.tar.gz" -C dist speq

      - name: Generate checksum
        shell: bash
        run: |
          set -euo pipefail
          cd dist
          shasum -a 256 "speq-${{ matrix.platform }}-${{ matrix.arch }}.tar.gz" > "speq-${{ matrix.platform }}-${{ matrix.arch }}.tar.gz.sha256"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.platform }}-${{ matrix.arch }}
          path: |
            dist/speq-${{ matrix.platform }}-${{ matrix.arch }}.tar.gz
            dist/speq-${{ matrix.platform }}-${{ matrix.arch }}.tar.gz.sha256

  publish-release:
    name: publish github release
    runs-on: ubuntu-latest
    needs: build-assets
    steps:
      - name: Download packaged assets
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Resolve release tag
        id: tag
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "value=${{ inputs.tag }}" >> "$GITHUB_OUTPUT"
          else
            echo "value=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
          fi

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.tag.outputs.value }}
          PRERELEASE: ${{ inputs.prerelease }}
          REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          args=()
          if [[ "${{ github.event_name }}" == "workflow_dispatch" && "${PRERELEASE}" == "true" ]]; then
            args+=(--prerelease)
          fi
          gh release create "$TAG" \
            -R "$REPO" \
            dist/speq-*.tar.gz \
            dist/speq-*.tar.gz.sha256 \
            --title "speq-cli $TAG" \
            --generate-notes \
            "${args[@]}"

      - name: Trigger homebrew tap formula update
        if: ${{ secrets.HOMEBREW_TAP_PAT != '' }}
        env:
          GH_TOKEN: ${{ secrets.HOMEBREW_TAP_PAT }}
          TAG: ${{ steps.tag.outputs.value }}
          TAP_REPO: speq-tms/homebrew-tap
        shell: bash
        run: |
          set -euo pipefail
          gh api "repos/${TAP_REPO}/dispatches" \
            -f event_type='speq-cli-release' \
            -f "client_payload[tag]=${TAG}" \
            -f "client_payload[source_repo]=${GITHUB_REPOSITORY}"

